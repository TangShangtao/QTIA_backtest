// #include "Logger.h"
// using namespace QB;
#include <clickhouse/client.h>

int main()
{
    // ToolKit::Logger::Init("./config/logger.yaml");
    // INFO("hello world");
    while (false)
    {
        
    }
}