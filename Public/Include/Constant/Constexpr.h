#pragma once

#include "Using.h"

#include <cinttypes>
#include <string>

namespace QB
{

// constexpr
constexpr static std::uint16_t MAX_SYMBOL_CODE_LEN = 32;
constexpr static std::uint32_t MAX_DEPTH_LEVEL = 25;
// constexpr static DeltaMilliSecs MAX_ONE_BATCH_SECS = 3600;
};